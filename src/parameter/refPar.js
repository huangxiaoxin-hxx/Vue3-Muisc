import { ref } from 'vue'

export const current = ref(0);
export const show = ref(false)
export const userDetail = ref(null)
export const likeList = ref(null)
export const playlist = ref(null)
export const clientHeight = ref(0)